<Window x:Class="DataBasePortfolio.DeleteValidationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:DataBasePortfolio.ViewModel"
        xmlns:local="clr-namespace:DataBasePortfolio"
        mc:Ignorable="d"
        Title="DeleteValidationWindow" Height="200" Width="500">
    <Window.DataContext>
        <vm:DeleteValidationWindowVM/>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="500"/>
            <ColumnDefinition Width="500"/>
            <ColumnDefinition Width="500"/>
        </Grid.ColumnDefinitions>
        <TextBox Margin="0,123,0,34" RenderTransformOrigin="0.5,0.5" Grid.ColumnSpan="2" x:Name="targetTextBox" Text="{Binding InputCode}"/>
        <Button Content="Cancel" HorizontalAlignment="Left" Height="22" Margin="26,155,0,0" VerticalAlignment="Top" Width="60" Click="Button_Click"/>
        <Button Content="Delete" HorizontalAlignment="Left" Height="22" Margin="437,155,0,0" VerticalAlignment="Top" Width="63" Click=""/>
        <TextBlock HorizontalAlignment="Left" Height="94" Margin="20,10,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="411" Grid.ColumnSpan="2" Text="レコードを削除する前に確認のため”DELETEROW”を入力し、Deleteを押して下さい"/>
    </Grid>
</Window>
