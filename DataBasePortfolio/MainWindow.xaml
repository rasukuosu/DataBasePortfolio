<Window x:Class="DataBasePortfolio.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:DataBasePortfolio.ViewModel"
        xmlns:local="clr-namespace:DataBasePortfolio"
        mc:Ignorable="d"
        Title="MainWindow" Height="1008" Width="1792"
        HorizontalAlignment="Stretch">
    <Window.DataContext>
        <vm:CompanyVM/>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="4" Margin="10,5,10,0" HorizontalAlignment="Center">
            <Run Language="ja-jp" Text="CompanyDB"/>
            <LineBreak/>
            <Run Language="ja-jp"/>
        </TextBlock>
        <!--入力フォーム-->
        <TextBlock Grid.Column="0" Grid.Row="1" Name="companyNameLabel">CompanyName</TextBlock>
        <TextBox Grid.Column="1" Grid.Row="1" Grid.ColumnSpan="3" Name="txtCompanyName" Text="{Binding Company.CompanyName, Mode=TwoWay}" />
        <TextBox Grid.Column="1" Grid.Row="2" Grid.ColumnSpan="3" Name="txtPresident" Text="{Binding Company.President, Mode=TwoWay}" />
        <TextBlock Grid.Column="0" Grid.Row="2" Name="presidentLabel">President</TextBlock>
        <!--<TextBox Grid.Row="3" Name="txtUrl" Grid.ColumnSpan="3" Margin="82,0,11,355" Grid.RowSpan="2"/>-->
        <Button Grid.Column="0" Grid.Row="4" Width="200" Height="200" Margin="16,108,16,642" Command="{Binding CreateCommand}">Create</Button>
        <Button Grid.Column="1" Grid.Row="4" Width="200" Height="200" Margin="25,108,25,642" Command="{Binding ReadCommand}">Read</Button>
        <Button Grid.Column="0" Grid.Row="4" Width="200" Height="200" Margin="16,157,16,118" Command="{Binding UpdateCommand}">Update</Button>
        <Button Grid.Column="1" Grid.Row="4" Width="200" Height="200" Margin="25,152,25,123" Command="{Binding DeleteWindowCommand}" RenderTransformOrigin="0.599,0.671">Delete</Button>
        <DataGrid Name="dataGrid" Grid.Column="2" Grid.Row="4" Width="1000" AutoGenerateColumns="False" ItemsSource="{Binding CompaniesList}" SelectedItem="{Binding Company}"  Margin="0,34,0,75">
            <DataGrid.Columns>

                <DataGridTextColumn Header="CompanyID" Binding="{Binding CompanyId}" />
                <DataGridTextColumn Header="Companyname" Binding="{Binding CompanyName}" />
                <DataGridTextColumn Header="President" Binding="{Binding President}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
